{
  "name": "kinolist-library-service",
  "version": "1.0.0",
  "description": "KinoList library service: favourites, watchlist and custom playlists with movie snapshots.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "test": "for f in tests/*.test.js; do node --import ./tests/env-setup.mjs \"$f\" || exit 1; done"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
