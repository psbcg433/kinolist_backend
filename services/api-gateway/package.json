{
  "name": "kinolist-api-gateway",
  "version": "1.0.0",
  "description": "KinoList browser-facing API gateway. Edge concerns only: routing, CORS, rate limiting, request IDs, health.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "test": "for f in tests/*.test.js; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "http-proxy-middleware": "^3.0.3",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
